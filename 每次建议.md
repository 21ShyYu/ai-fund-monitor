  另外你看到的 XGBoost warning 只是版本提示，不是致命错误。

  按下面修复即可。

  ### 1) 先在服务器同步分支并推送这次提交

  cd /root/ai-fund-monitor
  git status
  git pull --rebase origin main
  git push origin main

  cd /root/ai-fund-monitor
  git rebase --abort || true
  git fetch origin
  git reset --hard origin/main

  然后重新跑一次：

  cd /root/ai-fund-monitor/worker
  source .venv/bin/activate
  python -m scripts.run_once

  ### 3) 之后避免再次出现同样问题

  每次手动跑前先同步一次：

  cd /root/ai-fund-monitor
  git pull --rebase origin main
  cd worker
  source .venv/bin/activate
  python -m scripts.run_once

  如果你愿意，我下一步可以帮你把 git_auto_push 做成“push 失败自动 pull-rebase 再重试一次”，这样以后
  不会因为这个这个报错是正常的分支不同步问题，不是模型坏了。

  - xgboost 那段是警告，可先忽略。
  - 真正失败点：服务器本地先提交了 dashboard.json（d81093c），但远端 main 还有你本地刚推的新提交，
    所以 git push 被拒绝（non-fast-forward）。

  按下面执行即可修复：

  cd /root/ai-fund-monitor

  # 1) 看当前状态
  git status
  git log --oneline --decorate -n 8

  # 2) 先拉远端并把本地提交rebase上去
  git fetch origin
  git pull --rebase origin main

  如果这一步提示冲突（大概率在 data_exports/dashboard.json），直接用“保留本地最新导出”的方式：

  git checkout --ours data_exports/dashboard.json
  git add data_exports/dashboard.json
  git rebase --continue

  rebase 完成后：

  # 3) 推上去
  git push origin main

  然后再跑一次任务：

  cd /root/ai-fund-monitor/worker
  source .venv/bin/activate
  python -m scripts.run_once

  ———